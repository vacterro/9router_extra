PySide6>=6.6.0
httpx>=0.25.0
pytest>=7.0.0
pytest-asyncio>=0.21.0
# Optional password vault (Argon2id + AES-256-GCM). OS-backed secret access
# (Credential Manager / DPAPI) works without these.
cryptography>=42.0.0
argon2-cffi>=23.0.0
